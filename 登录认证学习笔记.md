# 登录认证学习笔记（JWT + 拦截器）

> 本文基于本项目（`D:\springbootlearning\demo`）的真实代码讲解。读完你会理解：登录接口怎么签发 token、拦截器怎么验 token、异常怎么统一转 JSON，以及将来怎么升级。

## 相关文件一览

| 文件 | 角色 | 一句话职责 |
|---|---|---|
| `src/main/java/com/example/demo/LoginController.java` | 发证机关 | 验证账号密码，通过就签发 token |
| `src/main/java/com/example/demo/JwtUtil.java` | 造证/验证的机器 | 把用户信息"封印"进 token；检查 token 真假/过期 |
| `src/main/java/com/example/demo/LoginInterceptor.java` | 门卫 | 每个请求进门先查证 |
| `src/main/java/com/example/demo/WebConfig.java` | 给门卫划地盘 | 哪些路径拦、哪些放行 |
| `src/main/java/com/example/demo/GlobalExceptionHandler.java` | 客服中心 | 所有异常统一转成 JSON |
| `src/main/java/com/example/demo/Result.java` | 信封 | 统一响应格式 `{code, msg, data}` |
| `src/main/java/com/example/demo/BusinessException.java` | 投诉单 | 业务异常（带 code） |
| `src/main/java/com/example/demo/LoginRequest.java` | 收件单 | 接收前端传来的 用户名/密码 |

---

## 0. 先看全局：4 个角色 + 一张图

现实类比：**登录一次拿到一张"盖章的通行证"，之后每次进大门（发请求）门卫只看证，不再问你叫什么。**

```
浏览器/前端
   │  ① POST /login {username,password}
   ▼
LoginController ──验证账号密码──> JwtUtil.generateToken() ──> 返回 {token:"eyJ..."}
   │
   │  ② 之后的每个请求都带上 Header: Authorization: <token>
   ▼
LoginInterceptor（门卫）──> JwtUtil.validateToken(token) ──真证──> 放行
   │                                            │
   │                                            └──假证/没证──> throw BusinessException(401)
   ▼                                                          └──> GlobalExceptionHandler 转 JSON
Controller → Service → Mapper → 数据库 → 结果包进 Result 返回
```

核心思想：**无状态** —— 服务器不存你的登录记录，验证全靠 token 本身。Session 要开"档案室"存每个登录用户，多台服务器部署时共享档案室很麻烦；JWT 任何一台服务器都能独立验证。

---

## 1. token 长什么样：解剖一个真实签发的 JWT

项目里实际签发过的一个 token：

```
eyJhbGciOiJIUzI1NiJ9 . eyJzdWIiOiJhZG1pbiIsInVzZXJJZCI6MSwiaWF0IjoxNzg3MTQ0MjM0LCJleHAiOjE3ODc3NDkwMzR9 . e1iCHYt9qm9zQoZPSfTVENzJLg2015IxZO4UNhA2J-Y
        └──── 段1 Header ────┘ └────────── 段2 Payload（数据）──────────┘ └──── 段3 Signature（签名）────┘
```

- **段1 Header**（Base64 解码后）：`{"alg":"HS256"}` —— 用了 HMAC-SHA256 签名算法
- **段2 Payload**（Base64 解码后）：`{"sub":"admin","userId":1,"iat":1787144234,"exp":1787749034}`
  - `sub` 用户名；`userId` 用户ID；`iat` 签发时间；`exp` 过期时间（本项目 = 签发时间 + 7 天）
- **段3 Signature** = `HMACSHA256(段1.段2, 密钥)`。服务器验证时用**同一个密钥**重算签名，对得上 = 没被篡改、是真证。

> ⚠️ 重要：**段2 只是 Base64 编码，不是加密，谁都能解开看**。所以 token 里绝不能放密码。签名（段3）的作用是"防篡改"，不是"保密"。
> 密钥在 `JwtUtil` 第 14 行（`"your-256-bit-secret-..."`），泄露密钥 = 任何人都能伪造 token。

---

## 2. `JwtUtil` 逐行讲

```java
// 第14-15行：密钥（>=32字节，HS256 要求）→ 生成签名 Key
private static final String SECRET = "your-256-bit-secret-your-256-bit-secret";
private static final Key key = Keys.hmacShaKeyFor(SECRET.getBytes());

// 第18行：有效期 7 天（毫秒）
private static final long EXPIRE_TIME = 1000 * 60 * 60 * 24 * 7;
```

**造证** `generateToken(userId, username)` —— 往 token 里塞什么，由你决定：

```java
Jwts.builder()
    .setSubject(username)           // 段2 的 sub：用户名
    .claim("userId", userId)        // 自定义字段：用户ID（"谁在访问"就用它）
    .setIssuedAt(new Date())        // iat：签发时间
    .setExpiration(now + EXPIRE_TIME) // exp：过期时间
    .signWith(key, HS256)           // 段3：签名（防篡改的关键）
    .compact();                     // 拼成 三段.三段.三段 字符串
```

**验证** `validateToken(token)` —— 注意那个 try/catch：

```java
try { parseToken(token); return true; }  // 验签 + 验过期，全过 = 真证
catch (Exception e) { return false; }    // 任何一步失败 = 假证（被篡改/过期/格式错）
```

> 💡 这就是为什么传 token 时**不能加 `Bearer ` 前缀**：`parseClaimsJws` 要求输入就是纯 JWT，多了 `Bearer ` 会抛异常 → 返回 false → 401。（想支持 `Bearer ` 前缀？见文末练习 2。）

---

## 3. `LoginController`：登录接口

```java
@PostMapping("/login")
public Result<Map<String, String>> login(@RequestBody LoginRequest loginRequest) {
    // 现在写死 admin/123456（注释自己也写了"实际应该查数据库"）
    if ("admin".equals(用户名) && "123456".equals(密码)) {
        String token = JwtUtil.generateToken(1L, "admin");   // 签发
        data.put("token", token);
        return Result.success(data);                          // {code:200, data:{token:"..."}}
    } else {
        throw new BusinessException(401, "用户名或密码错误");   // 密码错 → 抛给全局异常处理器
    }
}
```

两个"将来要改"的坑（也是教学点）：

1. **`generateToken(1L, "admin")` 的 `1L` 是写死的** —— 本项目数据库里真实用户 ID 是 2、5、6（没有 1）。将来应查库拿到**真实用户**再签发（见文末练习 1）。
2. **密码是明文** —— 将来要存 BCrypt 哈希，不能明文比较（见文末练习 3）。

---

## 4. `LoginInterceptor`：门卫（关键）

```java
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
    // ① 从请求头取证
    String token = request.getHeader("Authorization");

    // ② 没证 or 假证 → 拦截
    if (token == null || !JwtUtil.validateToken(token)) {
        response.setStatus(401);                        // 把 HTTP 状态码设成 401
        throw new BusinessException(401, "请先登录");    // 抛异常，交给全局处理器转 JSON
    }
    return true;   // ③ 有真证 → 放行，请求继续往下走进 Controller
}
```

三个知识点：

- **拦截器是"门卫"，不是"业务"**：只负责"有没有有效证"；你是谁、能干什么，留给 Controller/Service。
- **拦截器里可以 throw，而且 `@RestControllerAdvice` 照样接得住**（本项目实测：不带 token 请求 `/users` 返回了 `{"code":401,"msg":"请先登录"}`）。原理：`preHandle` 抛的异常同样走 `DispatcherServlet` 的异常处理链。
- **`return true` 才放行**；拦截器方法执行顺序：`preHandle`（进门查证）→ Controller → `postHandle` / `afterCompletion`（出门）。

---

## 5. `WebConfig`：给门卫划地盘

```java
registry.addInterceptor(new LoginInterceptor())
        .addPathPatterns("/**")        // 拦截所有请求
        .excludePathPatterns(          // 但以下路径放行：
            "/login",                  //  登录接口本身（不然自己拦自己，永远登不上）
            "/doc.html",               //  knife4j 文档页面
            "/webjars/**",             //  文档页面用的静态资源
            "/v2/api-docs",            //  swagger 的 JSON 文档
            "/swagger-resources/**");  //  同上
```

思路：**默认全拦 + 白名单放行**。好处：新接口默认就是受保护的，不会忘了加。

> 之前 doc.html 里测 `/user/{id}` 返回 `401 请先登录` 就是这里的设计结果：业务接口不在白名单里，必须带证。正确调试姿势：先在「登录认证」调 `POST /login` 拿 token → 粘贴到接口的 `Authorization` 输入框（**不要加 `Bearer ` 前缀**）→ 再发送。

---

## 6. 响应链：Result + BusinessException + GlobalExceptionHandler

**HTTP 状态码 ≠ 业务状态码**，是两码事。本项目里 401 恰好有 2 种表现：

| 场景 | 代码路径 | 实际返回 |
|---|---|---|
| 登录密码错 | Controller 抛 `BusinessException(401,...)`，没人设 HTTP 状态 | **HTTP 200**，body `{"code":401,...}` |
| 没带 token | 拦截器先 `response.setStatus(401)` 再抛异常 | **HTTP 401**，body `{"code":401,...}` |

- **HTTP 状态码**：给浏览器/网关看的"传输层"状态。
- **`Result.code`**：你自己定义的"业务层"状态。
- 正式项目建议统一口径（比如业务失败也一律 HTTP 200、code 区分，或反过来）。现在两处不一致不算错，但要理解区别。

`GlobalExceptionHandler` 的三个 `@ExceptionHandler` 是"按异常类型分诊"：

- `BusinessException` → 用异常自带的 code 和 msg（业务错）
- `MethodArgumentNotValidException` → 400 + 第一条校验失败信息（参数校验错）
- `Exception` → 500 + 兜底文案（系统错）

`@RestControllerAdvice` = 拦截所有 Controller（和拦截器）的异常 + 自动把返回值转 JSON。

---

## 7. 完整走一遍（真实数据）

```
GET /user/2，Header: Authorization: <登录返回的token>

① WebConfig 的门卫启动 → LoginInterceptor.preHandle
② 取 Header → JwtUtil.validateToken → 验签✓ 没过期✓ → return true 放行
③ TestController.getUserById(2)
④ UserService.getUserById(2) → userMapper.selectById(2) → SELECT * FROM user WHERE id=2
⑤ 数据库返回 {id:2, username:"测试小李", phone:"13999999999"}
⑥ Result.success(数据) → Jackson 转 JSON → HTTP 200 ✅

（若第②步没证/假证 → throw BusinessException(401,"请先登录")
 → GlobalExceptionHandler 接住 → {"code":401,"msg":"请先登录"}）
```

---

## 8. 升级练习（由易到难）

**练习 1：登录查数据库（推荐先做）**
在 `UserService` 加方法，改 `LoginController` 用真实用户：

```java
// UserService
public User findByUsername(String username) {
    QueryWrapper<User> w = new QueryWrapper<>();
    w.eq("username", username);
    return userMapper.selectOne(w);   // 查不到返回 null
}

// LoginController 里改成：
User user = userService.findByUsername(loginRequest.getUsername());
if (user != null && "123456".equals(loginRequest.getPassword())) {
    String token = JwtUtil.generateToken(user.getId(), user.getUsername()); // 真实 ID！
    ...
}
```

（数据库还没有 password 字段的话，先加一列 `password varchar`；练习 3 再谈加密。）

**练习 2：支持 Bearer 前缀（前端规范写法）**
```java
// LoginInterceptor 里：
String token = request.getHeader("Authorization");
if (token != null && token.startsWith("Bearer ")) {
    token = token.substring(7);
}
```
之后前端就可以传 `Authorization: Bearer xxx`。

**练习 3：密码加密存储**
加依赖 `spring-security-crypto`（BCrypt），注册时 `BCrypt.hashpw(password)` 存库，登录时 `BCrypt.checkpw(明文, 库里的哈希)` 比较。JWT 只是"会话凭证"，密码安全是另一层，两个都要会。

**练习 4：按角色授权**
登录时往 token 里塞 `claim("role", "admin")`；拦截器 `parseToken(token)` 取出 role，判断"这个接口只有 admin 能调"。

---

## 9. 自测题

1. 为什么说 JWT 的 payload 不能放密码？（提示：段2 是 Base64 还是加密？）
2. `validateToken` 的 try/catch 都捕获了什么？
3. 如果 `WebConfig` 忘了排除 `/login`，会发生什么？
4. "HTTP 200 + code=401" 和 "HTTP 401 + code=401" 区别在哪？

**答案**（先自己想再看）：
1. 段2 只是 Base64 编码，任何人拿到 token 都能解开看到内容；签名只防篡改、不保密。
2. 验签失败（被篡改）、过期、字符串格式不对（比如多了 `Bearer `）等任何异常。
3. 登录请求自己也会被拦截器拦下——客户端还没 token 就调登录接口 → 永远 401，死循环。
4. 前者"传输层成功、业务层失败"（body 里 code 区分）；后者"传输层就失败了"。正式项目选一种统一。

---

## 附：怎么在本地动手验证

1. IDEA 启动 `DemoApplication`（或 `mvn spring-boot:run`，pom 已修好不会被 skip）。
2. 打开 http://localhost:8080/doc.html
3. 「登录认证」→ `POST /login`，body：`{"username":"admin","password":"123456"}` → 复制返回的 `data.token`
4. 「用户管理」→ 任一接口的 `Authorization` 输入框粘贴 token（**不要加 Bearer**）→ 发送，返回 200。
